import { ReactNode } from 'react';
import { BasicPaginationPropsType } from '../BasicPagination/BasicPagination';
export declare type CustomRowType = {
    value: ReactNode;
    justifyContent: string;
    padding?: string;
};
export declare type TableTitleType = {
    width: string;
    value: string;
    padding?: string;
};
export declare type TableRowType = Record<string, ReactNode | CustomRowType>;
export declare type BasicTablePropsType = {
    titles: TableTitleType[];
    rows: TableRowType[];
    minWidthTable: number | string;
    paginationOptions?: BasicPaginationPropsType;
};
/**
 * JSX Component( BasicTable )
 * Принимает пропсы :
 * @param {TableTitleType[]} props.titles - Заголовки таблицы ( обязательный )
 * @param {TableRowType[]} props.rows - Строки таблицы ( данные ) ( обязательный )
 * @param { number | string} props.minWidthTable - Минимальная ширина таблицы
 * @param {BasicPaginationPropsType} props.paginationOptions - Настройки пагинатора ( необязательный )
 */
export declare const BasicTable: (props: BasicTablePropsType) => JSX.Element;
