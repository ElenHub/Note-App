import './Loader.module.css'; // Подключите CSS для стилей

const Loader = () => {
    return (
        <div className="preloader">
            <div className="spinner"></div>
        </div>
    );
};

export default Loader;